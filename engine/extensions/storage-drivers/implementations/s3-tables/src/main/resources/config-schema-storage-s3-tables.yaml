---
# WARNING: Do not edit this file manually, it will be replaced with factory default content on every Spt run.
storage:
  auth:
    version: int
  checksum:
    enabled: boolean
    algorithm: string
  object:
    fsAccess: boolean
    tagging:
      enabled: boolean
      tags: map
    versioning: boolean
  s3tables:
    controlPlaneEndpoint: string
    bucket: string
    namespace: string
    tableName: string
    opMode: string
    targetFileSizeBytes: int
    ingestFileSizeBytes: int
    totalIngestBytes: int
    commitFreqMs: int
    compactionPollIntervalMs: int
    compactionPollTimeoutMs: int
    compactionToleranceFactor: double
    compactionStabilizationPolls: int
    namespaceCount: int
    tablesPerNamespace: int
