---
# WARNING: Do not edit this file manually it will be replaced with factory default content on every Spt run.
item:
  data:
    dedupable: boolean
    input:
      compressibility: double
      file: string
      layer:
        cache: int
        heap: boolean
        size: any
      seed: string
    ranges:
      concat: string
      fixed: list
      random: int
      threshold: any
    size: any
    verify: boolean
  input:
    file: string
    path: string
  naming:
    type: string
    prefix: string
    shards: int
    radix: int
    step: int
    seed: any
    length: int
  output:
    file: string
    path: string
  type: string
load:
  batch:
    size: int
  step:
    id: string
    idAutoGenerated: boolean
    limit:
      size: any
      time: any
    node:
      addrs: list
      port: int
  service:
    threads: int
  op:
    limit:
      count: long
      fail:
        count: long
        rate: boolean
      rate: double
      recycle: int
    output:
      duplicates: boolean
    recycle:
      mode: boolean
      content:
        update: boolean
    retry: boolean
    retryLimit: int
    shuffle: boolean
    type: string
    delete:
      standalone: boolean
      batchSize: int
      duration: boolean
      selectionOrder: string
      selected: long
      selectedCurrentKey: long
      selectedExactVersion: long
      selectedBuckets: list
      seedMillis: long
      discoveryMillis: long
      workflowStartedEpochNanos: long
      preValidation: boolean
      postVerification: boolean
      verificationTimeoutMillis: long
    failureBudget:
      mode: string
      maxFailedObjects: long
      maxFailurePercent: double
      graceSeconds: long
    list:
      delimiter: string
      fetch_metadata: boolean
      include_versions: boolean
      max_keys: int
      sharding:
        mode: string
        radix: int
        charset: string
        delimiters: string
        split_pages: int
        split:
          log:
            rate: int
            sample: int
          pages: int
        split_log_rate: int
        split_log_sample: int
        queue_max: int
        summary: boolean
    wait:
      finish: boolean
      limit: int
output:
  color: boolean
  metrics:
    average:
      aggregation:
        period: int
      period: any
      persist: boolean
      table:
        header:
          period: int
    summary:
      persist: boolean
    trace:
      persist: boolean
    threshold: double
    timing:
      persist: boolean
    quantiles: list
run:
  id: long
  cluster:
    id: string
  comment: string
  node: boolean
  port: int
  scenario: string
  version: string
log:
  level: string
server:
  metrics:
    expose_fleet: boolean
api:
  linger:
    sec: int
storage:
  auth:
    file: string
    secret: string
    token: string
    uid: string
  driver:
    limit:
      concurrency: int
      multipart:
        objects: int
        parts: int
      queue:
        input: int
    threads: int
    type: string
  integrity:
    algorithm: string
    input:
      expectedProducerId: string
      provenance: string
    mode: string
    output:
      requireExactCount: boolean
    selection:
      maxCount: long
      requireNonEmpty: boolean
  namespace: string
  net:
    node:
      slice: boolean
aliasing: list
