package com.dell.spt.base.item.op.deletion;

/** Classification kept separate from the generic request success/failure metric. */
public enum DeleteFailureClassification {
	NONE, OPERATIONAL, PROTOCOL
}
